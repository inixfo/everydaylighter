# N8N Freelancer Demo Landing Page

Demo V2 package for Learn by Bluxor landing pages.

Upload:

```text
landing-page-sdk/demo-n8n-freelancer/dist-package/landing-page.zip
```

Suggested slug:

```text
n8n-freelancer-demo
```

The page uses only HTML, CSS, and static SVG assets. Learn by Bluxor injects the trusted runtime when serving `/go/{slug}`.
